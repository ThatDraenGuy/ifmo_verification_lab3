{% set PROC_COUNT = 5 %}
{% set SEM_COUNT = 1 %}

{% include './sem_base.smv' %}




-- если один процесс вошёл - другие не могут быть активными в тот же момент
  {% for i in range(PROC_COUNT) %}
  
    CTLSPEC
      AG (proc{{i}}.state = critical ->
        {% for j in range(PROC_COUNT) if j != i %}
        proc{{j}}.state != critical &
        {% endfor %} TRUE
     );
  {% endfor %}

